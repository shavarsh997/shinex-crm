-- The application uses one opaque database session token and has no OAuth,
-- access-token, refresh-token, or verification-token flow.
DROP TABLE IF EXISTS "Account";
DROP TABLE IF EXISTS "VerificationToken";
